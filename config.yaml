# Connection Enpoints
sources:
  dani_group_mysql:
    dialect: "mysql+pymysql"
    host: "danigroup.co.za" # or server IP
    port: 3306
    database: "danigrou_danisite_db"
    username: "danigrou_admin"
    password: "Gudi123!" # Replace with your password

targets:
  local_postgres:
    connection_uri: "postgresql://localhost/postgres"

# Medallion Engine Paths
directories:
  raw: "Data/raw"
  silver: "Data/silver"
  gold: "Data/gold"

# Capstone Ingestion Targets
ingestion:
  - source_id: "dani_group_mysql"
    source_query: "SELECT * FROM products;"
    target_table: "bronze_products"
    